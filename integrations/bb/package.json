{
  "name": "bb-plugin-murk",
  "version": "0.1.0",
  "type": "module",
  "description": "Scoped murk secrets for bb agent threads: value-free schema, grant-gated reads, dotenv delivery.",
  "license": "MIT OR Apache-2.0",
  "engines": {
    "bb": ">=0.40",
    "bbPluginSdk": ">=0.4.21"
  },
  "bb": {
    "name": "Murk",
    "description": "Grant-scoped secrets for agent threads, backed by a murk vault. Agents see key names and tags; values arrive as short-lived 0600 dotenv files unless a grant explicitly allows reveal.",
    "branding": {
      "icon": "./assets/icon.svg"
    },
    "server": "./server.ts",
    "skills": [
      "skills"
    ]
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@interrupted/murk-secrets": "^0.10.3 || ^0.11.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "0.4.21",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^26.4.1",
    "better-sqlite3": "^12.0.0",
    "cron-parser": "^5.10.0",
    "hono": "^4.11.9",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
